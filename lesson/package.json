{
  "name": "backend-lessons-live",
  "private": true,
  "type": "module",
  "description": "The project coded live in a session. A real client and server pair, the same shape as the class project.",
  "scripts": {
    "session": "node scripts/session.mjs",
    "setup": "npm --prefix server run setup",
    "server": "npm --prefix server start",
    "client": "npm --prefix client run dev",
    "install:all": "npm --prefix server install && npm --prefix client install"
  }
}
