// Lesson 2 does not touch the client. This is here so the page still loads if
// somebody opens it, and points at where the work actually is.

function AnimalList() {
  return <p>Lesson 2 is server side. The file being written is server/src/index.js.</p>;
}

export default AnimalList;
