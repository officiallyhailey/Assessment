-- LESSON 1.  Write the four steps.  Paste into DB Fiddle, PostgreSQL.


-- ── STEP 1 ──  Create a table called animals with these columns:
--   id          auto number, primary key
--   name        text (100), required, no duplicates
--   category    text (50), optional
--   can_fly     true/false, required
--   lives_in    text (100), optional
--   population  whole number




-- ── STEP 2 ──  Add three rows.  Leave id out, the database fills it in.
--   Lion,    Mammal, no,  Savanna,    23000
--   Penguin, Bird,   no,  Antarctica, 1200000
--   Eagle,   Bird,   yes, Mountains,  5000



-- ── STEP 3 ──  Read every column of every row.



-- ── STEP 4a ──  Every column, only the Lion row.



-- ── STEP 4b ──  Only name and lives_in, only animals that can fly.


